<UserControl x:Class="GUI.View.HoaDon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.View"
             mc:Ignorable="d" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}"
             Style="{StaticResource Page_Style}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <materialDesign:Card Grid.Row="0" Margin="5">
            <WrapPanel>
                <Button Margin="5" Background="#FF00E676">
                    <materialDesign:PackIcon
            Width="50"
            Height="25"
            Kind="PlusThick"/>
                </Button>
                <Button Margin="5">
                    <materialDesign:PackIcon
            Width="50"
            Height="25"
            Kind="Pencil"/>
                </Button>
                <Button Margin="5" Background="#FFF02929">
                    <materialDesign:PackIcon
            Width="50"
            Height="25"
            Kind="DeleteOutline"/>
                </Button>
                <Button Margin="5" Background="#FF00E676">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <materialDesign:PackIcon Width="50" Height="25" Kind="Printer" />
                        </Grid>
                        <Grid Grid.Column="1">
                            <TextBlock Text="In Hóa Đơn" VerticalAlignment="Center"></TextBlock></Grid>
                    </Grid>
                </Button>
            </WrapPanel>
        </materialDesign:Card>
        <materialDesign:Card Grid.Row="1" Margin="5" VerticalAlignment="Stretch">
            <ListView ItemsSource="{Binding List}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Tên Phòng" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Tiền Phòng" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Tiền Điện" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Tiền Nước" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Tổng cộng" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Cần Thu" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                        <GridViewColumn Header="Trạng Thái" DisplayMemberBinding="{Binding tenPhong}"></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </materialDesign:Card>
    </Grid>
</UserControl>
